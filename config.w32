// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("arrayex", "for arrayex support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("arrayex", "enable arrayex support", "no");

if (PHP_ARRAYEX != "no") {
	EXTENSION("arrayex", "arrayex.c");
}

